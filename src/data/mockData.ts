import { User, Listing, Bid, Transaction, AppNotification } from '../types';

// All fake demo/mock seed data has been removed per production requirements.
// The EcoNexus application connects to Cloud Firestore (project: econexus-25f65).
export const INITIAL_USERS: User[] = [];
export const INITIAL_LISTINGS: Listing[] = [];
export const INITIAL_BIDS: Bid[] = [];
export const INITIAL_TRANSACTIONS: Transaction[] = [];
export const INITIAL_NOTIFICATIONS: AppNotification[] = [];
